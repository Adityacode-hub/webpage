source 'https://rubygems.org'

# Use the GitHub Pages gem to match GitHub's Jekyll environment
gem "github-pages", group: :jekyll_plugins

# If you still need rexml for something else, keep it; otherwise remove it
gem "rexml", "~> 3.4"
